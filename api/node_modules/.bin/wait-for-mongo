#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../wait-for-mongo/bin/wait-for-mongo" "$@"
  ret=$?
else 
  node  "$basedir/../wait-for-mongo/bin/wait-for-mongo" "$@"
  ret=$?
fi
exit $ret
